#ifndef ALT_TASTYFILE
#define ALT_TASTYFILE

#include <string>
#include <stddef.h>

std::string int_toString(const int& self);
void int_addString(const int& self, std::string& a);
size_t string_test(const std::string& self);
int int_withOne(const int& self);

#endif