#include "main.hpp"

#include <vector>

void main() {
	std::vector<int> vec(32);
	vec.push_back(32);

	for(auto& i : vec) {
		
	}

	for(int i = 0; i < vec.size(); i += 2) {
		
	}

	for(int j = 10; i > 0; i--) {
		
	}
}